using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Photon;
using Photon.Pun;
public class GameSceneReloader : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //object playerSelectionNumber;
        
        //if (PhotonNetwork.LocalPlayer.CustomProperties.TryGetValue(Constants.PLAYER_LEVEL, out playerSelectionNumber))
        //{

        //    PhotonNetwork.LoadLevel((string)playerSelectionNumber);


        //}
    }

    
}
